apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: k8s-example-com-wildcard
  namespace: istio-system
spec:
  secretName: k8s-example-com-wildcard-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
  - "*.k8s.example.com"
