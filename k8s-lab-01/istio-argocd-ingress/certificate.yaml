apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: k8s-bevz-net-wildcard
  namespace: istio-system
spec:
  secretName: k8s-bevz-net-wildcard-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
  - "*.k8s.bevz.net"
